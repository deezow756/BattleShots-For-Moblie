<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="BattleShots.MainPage"
             x:Name="contentPage"
             NavigationPage.HasNavigationBar="False">

    <StackLayout x:Name="MainStack">
        <Button x:Name="btnKnownDevices" Text="Players Already Played" Clicked="BtnKnownDevices_Clicked"/>
        <ListView x:Name="lstPairedDevices" ItemTapped="LstPairedDevices_ItemTapped">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell Tapped="ViewCellKnown_Tapped" ClassId="{Binding Name}">
                        <StackLayout Orientation="Horizontal">
                            <Label Text="{Binding Name}" TextColor="{Binding TextColour}" FontSize="Large" HorizontalOptions="Center" VerticalOptions="Center"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Image x:Name="loadingImage" Source="LoadingImage.png" IsVisible="False"/>
        <Button x:Name="btnStartScan" Text="Look For New Players" Clicked="BtnStartScan_Clicked"/>
        <ListView x:Name="lstDiscoveredDevices" ItemTapped="LstDiscoveredDevices_ItemTapped">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell Tapped="ViewCell_Tapped" ClassId="{Binding Name}">
                        <StackLayout Orientation="Horizontal">
                            <Label Text="{Binding Name}" TextColor="{Binding TextColour}" FontSize="Large" HorizontalOptions="Center" VerticalOptions="Center"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button x:Name="btnDiscoverable" Text="Enable Discoverable" Clicked="BtnDiscoverable_Clicked"/>
    </StackLayout>

</ContentPage>
